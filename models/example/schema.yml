
version: 2

models:
    - name: fct_sales
      description: "Sales Table"
      columns:
          - name: Reference ID
            description: "The primary key for this table"
            tests:
                - unique
                - not_null